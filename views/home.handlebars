{{#each blog as |blog| }}
  <div>
    <h2>
      <a href="/blogpost/{{blog.id}}">{{blog.title}}</a>
    </h2>

    <p>Created by {{blog.user.name}} on {{ format_date blog.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{blog.contents}}
    </p>
  </div>
{{/each}}