{{#each posts}}
  <div>
    <h2>{{title}}</h2>
    <p>{{content}}</p>
    <p>Posted by: {{user.username}}</p>
    <ul>
      {{#each comments}}
        <li>{{this.content}} - {{this.user.username}}</li>
      {{/each}}
    </ul>
  </div>
{{/each}}